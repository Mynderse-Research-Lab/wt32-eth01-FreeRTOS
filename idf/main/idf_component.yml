dependencies:
  espressif/arduino-esp32: "^3.0.0"
  # Note: AsyncMqttClient and AsyncTCP are included via arduino-esp32 managed components
  # If they are not available, you may need to add them manually to managed_components/
