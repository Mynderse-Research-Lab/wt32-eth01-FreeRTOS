<?xml version="1.0" ?>
<!-- 
  DO NOT EDIT gantry_cell.sdf DIRECTLY.
  This file is automatically generated from gantry_cell.sdf.template and datums.csv.
-->
<sdf version="1.7">
  <world name="battery_sorting_cell">
    
    <!-- Global Origin Offset (Included for completeness) -->
    <model name="CELL-ORIGIN">
      <pose>{{DATUM_CELL_ORIGIN_X}} {{DATUM_CELL_ORIGIN_Y}} {{DATUM_CELL_ORIGIN_Z}} 0 0 0</pose>
    </model>

    <!-- Vision System: Camera -->
    <model name="VIS-01">
      <!-- Note: SDF coordinates are in meters. The generator script divides CAD mm by 1000. -->
      <pose>{{DATUM_CAM_LENS_X}} {{DATUM_CAM_LENS_Y}} {{DATUM_CAM_LENS_Z}} 0 0 0</pose>
      <link name="camera_link">
        <sensor name="overhead_camera" type="camera">
          <!-- Add camera intrinsics / physics here -->
        </sensor>
      </link>
    </model>

    <!-- Conveyor System: Pick Plane -->
    <model name="CONV-01">
      <pose>{{DATUM_PICK_PLANE_X}} {{DATUM_PICK_PLANE_Y}} {{DATUM_PICK_PLANE_Z}} 0 0 0</pose>
      <link name="pick_plane_link">
        <!-- Physical bounds of the conveyor tracking zone -->
      </link>
    </model>

    <!-- Gantry X Axis -->
    <model name="AX-X">
      <pose>{{DATUM_AXIS_X_HOME_X}} {{DATUM_AXIS_X_HOME_Y}} {{DATUM_AXIS_X_HOME_Z}} 0 0 0</pose>
      <!-- Base structure, motors, limit switches go here -->
    </model>

    <!-- Gantry Z Axis -->
    <model name="AX-Z">
      <pose>{{DATUM_AXIS_Z_HOME_X}} {{DATUM_AXIS_Z_HOME_Y}} {{DATUM_AXIS_Z_HOME_Z}} 0 0 0</pose>
      <!-- Vertical carriage and limits go here -->
    </model>

  </world>
</sdf>
