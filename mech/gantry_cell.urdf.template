<?xml version="1.0"?>
<!-- 
  DO NOT EDIT gantry_cell.urdf DIRECTLY.
  This file is automatically generated from gantry_cell.urdf.template and datums.csv.
-->
<robot name="gantry_cell">
  
  <!-- The absolute origin of the cell (FIRMWARE_ORIGIN) -->
  <link name="base_link" />

  <!-- Vision System: Camera -->
  <link name="VIS-01">
    <visual>
      <!-- Because the STL is exported relative to the DATUM in SolidWorks, visual offset is always 0 -->
      <origin xyz="0 0 0" rpy="0 0 0" />
      <geometry>
        <mesh filename="meshes/VIS-01.stl"/>
      </geometry>
    </visual>
  </link>

  <joint name="base_to_camera" type="fixed">
    <parent link="base_link"/>
    <child link="VIS-01"/>
    <!-- Python script inserts X, Y, Z (meters) and Roll, Pitch, Yaw (radians) -->
    <origin xyz="{{DATUM_CAM_LENS_X}} {{DATUM_CAM_LENS_Y}} {{DATUM_CAM_LENS_Z}}" rpy="{{DATUM_CAM_LENS_ROLL}} {{DATUM_CAM_LENS_PITCH}} {{DATUM_CAM_LENS_YAW}}" />
  </joint>

  <!-- Conveyor System: Pick Plane -->
  <link name="CONV-01">
    <visual>
      <origin xyz="0 0 0" rpy="0 0 0" />
      <geometry>
        <mesh filename="meshes/CONV-01.stl"/>
      </geometry>
    </visual>
  </link>

  <joint name="base_to_pick_plane" type="fixed">
    <parent link="base_link"/>
    <child link="CONV-01"/>
    <origin xyz="{{DATUM_PICK_PLANE_X}} {{DATUM_PICK_PLANE_Y}} {{DATUM_PICK_PLANE_Z}}" rpy="{{DATUM_PICK_PLANE_ROLL}} {{DATUM_PICK_PLANE_PITCH}} {{DATUM_PICK_PLANE_YAW}}" />
  </joint>

  <!-- Gantry X Axis -->
  <link name="AX-X">
    <visual>
      <origin xyz="0 0 0" rpy="0 0 0" />
      <geometry>
        <mesh filename="meshes/AX-X.stl"/>
      </geometry>
    </visual>
  </link>

  <joint name="base_to_axis_x" type="fixed">
    <parent link="base_link"/>
    <child link="AX-X"/>
    <origin xyz="{{DATUM_AXIS_X_HOME_X}} {{DATUM_AXIS_X_HOME_Y}} {{DATUM_AXIS_X_HOME_Z}}" rpy="{{DATUM_AXIS_X_HOME_ROLL}} {{DATUM_AXIS_X_HOME_PITCH}} {{DATUM_AXIS_X_HOME_YAW}}" />
  </joint>

  <!-- Gantry Z Axis -->
  <link name="AX-Z">
    <visual>
      <origin xyz="0 0 0" rpy="0 0 0" />
      <geometry>
        <mesh filename="meshes/AX-Z.stl"/>
      </geometry>
    </visual>
  </link>

  <joint name="base_to_axis_z" type="fixed">
    <parent link="base_link"/>
    <child link="AX-Z"/>
    <origin xyz="{{DATUM_AXIS_Z_HOME_X}} {{DATUM_AXIS_Z_HOME_Y}} {{DATUM_AXIS_Z_HOME_Z}}" rpy="{{DATUM_AXIS_Z_HOME_ROLL}} {{DATUM_AXIS_Z_HOME_PITCH}} {{DATUM_AXIS_Z_HOME_YAW}}" />
  </joint>

  <!-- End Effector / Tool Center Point -->
  <link name="EFF-01">
    <visual>
      <origin xyz="0 0 0" rpy="0 0 0" />
      <geometry>
        <mesh filename="meshes/EFF-01.stl"/>
      </geometry>
    </visual>
  </link>

  <joint name="base_to_tooling" type="fixed">
    <parent link="base_link"/>
    <child link="EFF-01"/>
    <origin xyz="{{DATUM_TOOL_CENTER_POINT_X}} {{DATUM_TOOL_CENTER_POINT_Y}} {{DATUM_TOOL_CENTER_POINT_Z}}" rpy="{{DATUM_TOOL_CENTER_POINT_ROLL}} {{DATUM_TOOL_CENTER_POINT_PITCH}} {{DATUM_TOOL_CENTER_POINT_YAW}}" />
  </joint>

</robot>
